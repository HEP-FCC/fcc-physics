# - Config file for the albers package
# It defines the following variables
#  ALBERS_INCLUDE_DIRS - include directories for albers
#  ALBERS_LIBRARIES    - libraries to link against
#  ALBERS_EXECUTABLE   - the bar executable

# Compute paths
get_filename_component(ALBERS_CMAKE_DIR "${CMAKE_CURRENT_LIST_FILE}" PATH)
set(ALBERS_INCLUDE_DIRS "@CONF_INCLUDE_DIRS@")

# Our library dependencies (contains definitions for IMPORTED targets)
include("${ALBERS_CMAKE_DIR}/albersTargets.cmake")

# These are IMPORTED targets created by albersTargets.cmake
set(ALBERS_LIBRARIES foo)
set(ALBERS_EXECUTABLE bar)
